---
apiVersion: apps/v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    matchLabels:
      app: backend
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
